# Five-minute demo script

1. Explain that payment validity does not guarantee decision validity.
2. Enter the provided outdoor basketball-shoe request with guard off; show manipulated baseline selection.
3. Repeat with guard on, explicitly confirm the structured intent, and show intervention evidence.
4. Run a benign catalogue slice and show that only `ALLOW` exposes final purchase confirmation.
5. Show the test order identifier and run `python -m audit.verify`.
6. Run the held-out evaluation and read numbers only from its `summary.json`.
7. Close with limitations: synthetic data, trusted structured fields, and imperfect detection.

